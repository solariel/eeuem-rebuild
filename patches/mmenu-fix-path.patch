diff --git a/README.md b/README.md
index 357ce3a..ec24cdf 100644
--- a/README.md
+++ b/README.md
@@ -33,7 +33,7 @@ plugin. You need to [download an 8.x.x
 version](https://github.com/FrDH/mmenu-js/releases) of the library and place it
 in `/libraries` in your docroot (create the directory if you need to). Rename
 your newly placed download to mmenu, so the resulting path is
-`/libraries/mmenu`. This module will look in `/libraries/mmenu/dist` for the
+`/libraries/mmenu-js`. This module will look in `/libraries/mmenu-js/dist` for the
 javascript files so ensure you have the correct file structure.
 
 The other optional library which adds functionality (if desired) is the
diff --git a/drupalci.yml b/drupalci.yml
index 13d4a91..31420bd 100644
--- a/drupalci.yml
+++ b/drupalci.yml
@@ -20,7 +20,7 @@ build:
           - sudo -u www-data mkdir libraries
           - sudo -u www-data curl -L $(curl -s https://api.github.com/repos/FrDH/mmenu-js/releases/latest | grep tarball_url | cut -d '"' -f4) --output mmenu.tar.gz --silent
           - sudo -u www-data tar xzf mmenu.tar.gz
-          - sudo -u www-data mv FrDH-mmenu* libraries/mmenu
+          - sudo -u www-data mv FrDH-mmenu* libraries/mmenu-js
           - sudo -u www-data curl -L https://github.com/joeldbirch/superfish/archive/v1.7.10.tar.gz --output superfish.tar.gz --silent
           - sudo -u www-data tar xzf superfish.tar.gz
           - sudo -u www-data mv superfish-1.7.10 libraries/superfish
diff --git a/responsive_menu.install b/responsive_menu.install
index 461cd37..0b32ce6 100644
--- a/responsive_menu.install
+++ b/responsive_menu.install
@@ -134,8 +134,8 @@ function responsive_menu_requirements($phase) {
     'mmenu' => [
       'title' => 'mmenu library',
       'version' => '8.4.0',
-      'path' => '/libraries/mmenu',
-      'version_path' => '/libraries/mmenu/composer.json',
+      'path' => '/libraries/mmenu-js',
+      'version_path' => '/libraries/mmenu-js/composer.json',
       'severity' => REQUIREMENT_ERROR,
       'download_url' => 'https://github.com/FrDH/mmenu-js/releases/latest',
       'homepage_url' => 'http://mmenu.frebsite.nl',
diff --git a/responsive_menu.libraries.yml b/responsive_menu.libraries.yml
index 8422aaa..77213c2 100644
--- a/responsive_menu.libraries.yml
+++ b/responsive_menu.libraries.yml
@@ -7,10 +7,10 @@ responsive_menu.styling:
 responsive_menu.mmenu:
   version: VERSION
   js:
-    /libraries/mmenu/dist/mmenu.js: {}
+    /libraries/mmenu-js/dist/mmenu.js: {}
   css:
     theme:
-      /libraries/mmenu/dist/mmenu.css: {}
+      /libraries/mmenu-js/dist/mmenu.css: {}
 
 responsive_menu.config:
   version: VERSION
@@ -41,7 +41,7 @@ responsive_menu.superfish_hoverintent:
 responsive_menu.polyfills:
   version: VERSION
   js:
-    /libraries/mmenu/dist/mmenu.polyfills.js: {}
+    /libraries/mmenu-js/dist/mmenu.polyfills.js: {}
 
 responsive_menu.toolbar:
   version: VERSION
diff --git a/responsive_menu.module b/responsive_menu.module
index 8a430e8..3f64569 100644
--- a/responsive_menu.module
+++ b/responsive_menu.module
@@ -142,7 +142,7 @@ function responsive_menu_page_bottom(&$page) {
   // Note that in the past the final build files have not included the
   // polyfills file (see d.o issues #3143984, #3111949) so we need to
   // check if its actually there to avoid a 404.
-  if ($config->get('use_polyfills') && file_exists(DRUPAL_ROOT . '/libraries/mmenu/dist/mmenu.polyfills.js')) {
+  if ($config->get('use_polyfills') && file_exists(DRUPAL_ROOT . '/libraries/mmenu-js/dist/mmenu.polyfills.js')) {
     $output['#attached']['library'][] = 'responsive_menu/responsive_menu.polyfills';
   }
 
